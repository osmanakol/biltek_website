<div class="events-form-circles">
    <div class="events-form-left-circles-one"></div>
    <div class="events-form-left-circles-two"></div>
    <div class="events-form-left-circles-three"></div>
    <div class="events-form-right-circles-one"></div>
    <div class="events-form-right-circles-two"></div>
    <div class="events-form-right-circles-three"></div>
</div>

<div class="events-form">
    <div class="events-form-title">
        <h2>{{data.0.text}}</h2>
    </div>
    <div class="events-form-content">
        <div class="events-form-conctent-form">
            <form>
                <div class="events-form-content-information">
                    <input id="{{data.1.class.id}}" type="{{data.1.class.type}}" required="" name="">
                    <{{data.1.child.span.type}}>{{data.1.child.span.text}}</{{data.1.child.span.type}}>
                </div>
                <div class="events-form-content-information">
                    <input id="{{data.2.class.id}}" type="{{data.2.class.type}}" required="" name="">
                    <{{data.2.child.span.type}}>{{data.2.child.span.text}}</{{data.2.child.span.type}}>
                </div>
                {{!-- <div class="events-form-content-information">
                    <input id="phoneNumber" type="tel" required="" name="">
                    <span>Telefon</span>
                </div> --}}
                <div class="events-form-content-information">
                    <select id="{{data.3.class.id}}" name="{{data.3.class.name}}" disabled
                        onchange="{{data.3.functions.onchange}}" required="">
                        <{{data.3.child.option.type}}>{{data.3.child.option.text}}</{{data.3.child.option.type}}>
                    </select>
                </div>
                <div class="events-form-content-information">
                    <select id="{{data.4.class.id}}" name="{{data.4.class.name}}" onclick="" onchange="fillClass()" required="">
                        <{{data.4.child.option.type}}>{{data.4.child.option.text}}</{{data.4.child.option.type}}>
                    </select>
                </div>
                <div class="events-form-content-information">
                    <select id="{{data.5.class.id}}" name="{{data.5.class.name}}" onchange="fillTeam()" required="">
                        <{{data.5.child.option.type}}>{{data.5.child.option.text}}</{{data.5.child.option.type}}>
                    </select>
                </div>
                <div class="events-form-content-information">
                    <select id="{{data.6.class.id}}" name="{{data.6.class.name}}" required="">
                        <{{data.6.child.option.type}}>{{data.6.child.option.text}}</{{data.6.child.option.type}}>
                    </select>
                </div>
            </form>
            <button class="events-form-button" type="button" onclick="submit_validation()">Kayıt Ol</button>
        </div>
    </div>
</div>